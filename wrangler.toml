# Cloudflare deploy config — serves the static landing site in docs/ as an
# assets-only Worker (no Worker script). `npx wrangler deploy` uploads docs/.
name = "repo-graph"
compatibility_date = "2024-11-01"

[assets]
directory = "./docs"
