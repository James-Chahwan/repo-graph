[package]
name = "repo-graph-code-domain"
version = "0.4.3"
edition = "2024"
publish = false

[dependencies]
repo-graph-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
thiserror = "2"
